const Constants = {
  // The iNaturalist API URL
  API_URL: "https://api.inaturalist.org/v1",
  // The iNaturalist API URL for the observations endpoint
  OBSERVATIONS_URL: "/observations",
  locale: {
    en: "en" as const,
    es: "es" as const,
  },
};

export default Constants;
